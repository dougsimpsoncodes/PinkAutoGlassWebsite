# 2026-03-08 — Daily Work Log

## Deployment & Cleanup — All Pending Changes Pushed

### What Was Done

1. **Pushed national satellite lead routing** (4 commits already staged)
   - `feat: fire admin alerts for in-market leads from national satellite sites`
   - `/audit-phones` skill + audit script
   - 4 Colorado satellite sites added to tracking config
   - Pre-push checks passed, deployed to Vercel

2. **Organized and committed all remaining unstaged/untracked changes** into 10 atomic commits:
   - **RC webhook security** — SMS webhook now uses URL secret (`auth_token` param) instead of unreliable per-event Validation-Token header. Auto-reply dedup switched to deterministic message_id per phone per day.
   - **Arizona market support** — Footer dynamically shows AZ phone `(480) 712-7465` on Arizona pages, CO phone elsewhere. Schema.org `telephone` param added for per-market structured data.
   - **Review scheduling** — All review request SMS anchored to 6 PM MT (skips Sunday). Follow-up emails get random 0–5 min stagger to avoid SendGrid rate limits. Temp emails (`@temp.pinkautoglass.com`) skipped by drip processor. `max_retries=5` added.
   - **Google reviews from Supabase** — Dashboard now reads from `google_reviews` table (synced via Places API) instead of making live API calls. Local curated set kept as fallback only. GBP reviews sync added to "Sync All Data" button.
   - **Invoice import enhancements** — Upload now auto-schedules review requests for newly completed leads and runs omega data cleanup on today's records.
   - **Admin dashboard revamp** — Main dashboard simplified to key metrics. Removed separate Google Ads, Microsoft Ads, and Ad Performance pages from nav. Added unified Search Ads Control page. All pages got "Market toggle coming soon" badge. DateFilterBar added to External Leads and Website pages. Satellite page gained conversion rate and insight panels.
   - **Misc improvements** — 25+ disposable email domains blocked in validation. Invoice parser upgraded from Gemini 1.5-flash to 2.5-flash. Pre-push hook simplified (check root URL, removed brand URL verification). Drip cron changed from daily to every 5 minutes.
   - **New features** — Search Ads Control page, GBP reviews sync endpoint, omega data flags endpoint, omega data cleanup lib, related Supabase migrations.
   - **Utility scripts** — MS Ads keyword management, GBP review sync testing, omega cleanup, spam lead deletion, negative keyword lists.
   - **Docs & tasks** — Admin revamp keep list, dashboard mockups, SEO/AEO strategy notes, daily logs, GridScope scan screenshot.

### Decisions Made
- Pushed the 4 satellite commits first (clean, coherent feature) before organizing the rest
- Supabase `.temp/` files skipped (gitignored)
- Grouped remaining changes into logical atomic commits rather than one big dump

### Verification
- All pre-push checks passed both pushes (build, schema validation)
- Working tree confirmed clean after final push
- 14 total commits pushed to origin/main today
