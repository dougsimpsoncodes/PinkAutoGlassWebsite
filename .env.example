# Pink Auto Glass Environment Configuration
# Copy this file to .env.local and fill in your actual values

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================

# Supabase project URL (from Supabase Dashboard > Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase anon/public key (from Supabase Dashboard > Settings > API)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-anon-key

# Supabase service role key (KEEP SECRET - SERVER SIDE ONLY)
# Used for admin operations that bypass RLS
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-service-role-key

# Postgres direct connection URL (REQUIRED for vehicle API endpoints)
# Get from Supabase Dashboard > Settings > Database > Connection string (Pooler)
# Format: postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-1-us-east-2.pooler.supabase.com:5432/postgres
POSTGRES_URL=postgresql://postgres.your-project-ref:your-password@aws-1-us-east-2.pooler.supabase.com:5432/postgres

# =============================================================================
# SITE CONFIGURATION
# =============================================================================

# Application URL (for generating links in emails/SMS)
NEXT_PUBLIC_SITE_URL=https://pinkautoglass.com
NEXT_PUBLIC_SITE_NAME="Pink Auto Glass"

# Contact Information
NEXT_PUBLIC_PHONE="+17209187465"
NEXT_PUBLIC_EMAIL="service@pinkautoglass.com"

# Environment (development, staging, production)
NODE_ENV=development

# =============================================================================
# STORAGE CONFIGURATION
# =============================================================================

# Supabase Storage bucket for lead media files
NEXT_PUBLIC_STORAGE_BUCKET_MEDIA=lead-media

# Maximum file size for uploads (in bytes) - 10MB default
NEXT_PUBLIC_MAX_FILE_SIZE=10485760

# Maximum number of files per upload
NEXT_PUBLIC_MAX_FILES_PER_UPLOAD=5

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================

# Rate limiting settings
RATE_LIMIT_MAX_REQUESTS=10
RATE_LIMIT_WINDOW_MS=60000

# Form integrity tokens (REQUIRED - generate with: openssl rand -hex 32)
FORM_INTEGRITY_SECRET=your-32-character-hex-key-for-form-token-signing
FINGERPRINT_SALT=your-32-character-hex-key-for-fingerprint-hashing
SALT_VERSION=v1

# Monitor-only mode (set to true for initial deployment to test without blocking)
SECURITY_MONITOR_ONLY=false

# Form session encryption key (generate with: openssl rand -hex 32)
FORM_SESSION_SECRET=your-32-character-hex-key-for-session-encryption

# File upload signature key (generate with: openssl rand -hex 32)
FILE_UPLOAD_SECRET=your-32-character-hex-key-for-file-signatures

# =============================================================================
# ADMIN AUTHENTICATION
# =============================================================================

# Simple password protection for admin dashboard
# Generate a password hash with: node -e "const bcrypt = require('bcryptjs'); bcrypt.hash('your-password', 10).then(console.log)"
ADMIN_PASSWORD_HASH=$2a$10$rqzX8qgKqYZHxE5yHx5z5.8FvYqPxJ5kL7WX8QxY5zX8qgKqYZHxE
ADMIN_SESSION_SECRET=your-random-32-character-secret-key-change-this

# =============================================================================
# COMMUNICATION SERVICES (Optional)
# =============================================================================

# RingCentral configuration for call tracking and SMS
# Get these from: https://developers.ringcentral.com/my-account.html
RINGCENTRAL_CLIENT_ID=your-ringcentral-client-id
RINGCENTRAL_CLIENT_SECRET=your-ringcentral-client-secret
RINGCENTRAL_JWT_TOKEN=your-ringcentral-jwt-token
RINGCENTRAL_PHONE_NUMBER=+1234567890
RINGCENTRAL_SERVER_URL=https://platform.ringcentral.com

# Twilio configuration for SMS notifications (backup to RingCentral)
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=+1234567890

# Admin notification settings
ADMIN_EMAIL=admin@pinkautoglass.com
ADMIN_PHONE=+1234567890

# SendGrid configuration for email notifications
SENDGRID_API_KEY=your-sendgrid-api-key
SENDGRID_FROM_EMAIL=noreply@pinkautoglass.com
SENDGRID_FROM_NAME=Pink Auto Glass

# Resend configuration for email notifications (Alternative to SendGrid - not used if using SendGrid)
RESEND_API_KEY=your-resend-api-key
FROM_EMAIL=noreply@pinkautoglass.com

# =============================================================================
# EXTERNAL SERVICES (Optional)
# =============================================================================

# Google Maps API for address validation and geocoding
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# reCAPTCHA for form protection
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=your-recaptcha-site-key
RECAPTCHA_SECRET_KEY=your-recaptcha-secret-key

# =============================================================================
# ANALYTICS AND MONITORING
# =============================================================================

# Google Analytics tracking ID
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Google Ads API for campaign tracking and ROI analysis
# Get these from: https://console.cloud.google.com/
# See GOOGLE_ADS_API_SETUP.md for detailed setup instructions
GOOGLE_ADS_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_ADS_CLIENT_SECRET=GOCSPX-your-client-secret
GOOGLE_ADS_REFRESH_TOKEN=1//your-refresh-token
GOOGLE_ADS_DEVELOPER_TOKEN=your-developer-token
GOOGLE_ADS_CUSTOMER_ID=1234567890

# Sentry DSN for error tracking
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# =============================================================================
# WEBHOOK CONFIGURATION
# =============================================================================

# Webhook URLs for external integrations
WEBHOOK_CRM_URL=https://your-crm-system.com/webhook/new-lead
WEBHOOK_SLACK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# Webhook authentication tokens
WEBHOOK_CRM_TOKEN=your-crm-webhook-token
WEBHOOK_SECRET_KEY=your-webhook-secret-for-verification

# =============================================================================
# DEVELOPMENT TOOLS
# =============================================================================

# Enable debug logging in development
DEBUG_MODE=true

# Mock external services in development
MOCK_SMS_SERVICE=true
MOCK_EMAIL_SERVICE=true
MOCK_PAYMENT_SERVICE=true

# =============================================================================
# PRODUCTION NOTES
# =============================================================================

# For production deployment:
# 1. Generate secure random keys for all SECRET variables
# 2. Use environment variables in your hosting platform (Vercel, Railway, etc.)
# 3. Never commit .env.local files to version control
# 4. Rotate keys regularly and audit access
# 5. Use different keys for different environments (dev, staging, prod)

# Security checklist:
# ✓ All SECRET keys are unique and randomly generated
# ✓ Service role key is only used server-side
# ✓ Rate limiting is configured appropriately
# ✓ CORS is properly configured in Supabase
# ✓ RLS policies are enabled and tested
# ✓ File upload validation is implemented
# ✓ Webhook endpoints are secured

# Required for basic functionality:
# - NEXT_PUBLIC_SUPABASE_URL
# - NEXT_PUBLIC_SUPABASE_ANON_KEY
# - SUPABASE_SERVICE_ROLE_KEY

# Optional for enhanced features:
# - All other variables can be added as needed