# PROJECT-STATE.md — Pink Auto Glass Website

## One-Line Summary
Live client site at `pinkautoglass.com`; as of 2026-04-12 the latest investigated CI/CD alert for commit `b8b7c5e` appears self-healed because the matching GitHub Actions runs are now green.

## What's Built
- [x] Production website and deployment pipeline live in this repo
- [x] GitHub Actions `CI/CD Pipeline` and `Security Checks` workflows active

## Current Focus
Automated quote engine MVP foundation (exact cash windshield quote when confident, estimate/manual review otherwise) AND ongoing measurement/attribution hardening so Denver paid/SEO decisions are based on stricter call attribution data. RingCentral recording-analysis pilot for Denver closed-vs-lost is scoped; secrets-safe local OAuth for Search Console is unblocked.

## Decisions Made
- 2026-04-12: Treat queued failure `ci_failure_210` as self-healed because `gh run view` for commit `b8b7c5e` showed the relevant `CI/CD Pipeline` run completed successfully, with no failed-step logs available.

## Known Issues
- `scripts/auto-fix/project-map.json` points PinkAutoGlassWebsite at a stale workspace path instead of this repo, which can misroute future auto-fix attempts.
- Working tree currently contains unrelated local changes in `tasks/todo.md` and `tasks/2026-04-12-google-ads-call-attribution-cascade-fix.md`; avoid touching them during automated fixes.

## Next Steps (priority order)
1. If resumed, finish won-cohort cleanup for the RingCentral pilot so the final comparison uses only true acquisition-win calls; then refresh the findings memo and scorecard accordingly.
2. Decide whether to operationalize `analysis/ringcentral-pilot/CALL-CLOSE-SOP.md` into a shorter rep cheat sheet and manager QA checklist.
3. Apply the new attribution-health snapshot migration + deploy the cron route so monitoring becomes live.
4. Apply the new `leads.session_id` backfill migration, then verify legacy reads can be narrowed further.
5. Keep dual-platform conflicts quarantined from bidding exports until stronger tie-break evidence exists.
6. Decide whether to make the attribution-health route snapshot yesterday-only or configurable for broader windows once live data accumulates.
7. Continue monitoring CI/CD alerts; only apply a code fix if a currently failing run reproduces.

## Key Files
- `.github/workflows/` — CI/CD and security workflows
- `PROJECT-STATE.md` — this project log
- `RINGCENTRAL-RECORDING-PILOT-SPEC.md` — locked pilot execution spec for Denver 20-call closed-vs-lost analysis
- `analysis/ringcentral-pilot/README.md` — folder-level closeout summary for the recording-analysis pilot
- `analysis/ringcentral-pilot/pilot-findings.md` — executive findings memo from the pilot
- `analysis/ringcentral-pilot/CALL-CLOSE-SOP.md` — operational sales-call SOP derived from the pilot
- `GSC-SECRETS-SAFE-OAUTH-SPEC.md` — approved implementation spec for the local-only Search Console OAuth exchange flow

## Credentials & Config
- GitHub Actions / Vercel secrets: stored in GitHub repo settings and Vercel project settings

## Integrations
- **Mygrant Glass API** — setup form drafted 2026-04-15; customer API access confirmed by Leon. Vendor contact: `api-support@mygrantglass.com` (Leon Staub / Mark Wright / Tim Veilleux). Mygrant does not IP-allowlist; traffic is identified by a required `User-Agent` header. Exact string and enforcement requirements live in `.claude/CLAUDE.md` under "Mygrant API Integration — User-Agent Rule". Client foundation now lives in `src/lib/mygrant/client.ts`; guard script: `npx tsx scripts/verify-mygrant-client.ts`.
- **PlateToVIN API** — selected as MVP plate lookup provider. Client foundation lives in `src/lib/platelookup/client.ts`; smoke script: `npx tsx scripts/platetovin-smoke.ts --plate=ABC123 --state=CO`.

## Change Log
- 2026-04-12: Created project state file in the active repo and logged self-healed investigation for `ci_failure_210`.
- 2026-04-14: Started call attribution remediation. Audited production data, identified the 3 ad_platform writers, found the orphaned Phase 2 resolver, drafted the 3-PR plan, ran reviews via Claude self-read + Gemini + Codex.
- 2026-04-15: PR1 merged (commit 3e9ac57). Migration applied to live DB. Dashboard builders updated. PR2 work scheduled for next session — see `tasks/2026-04-14-attribution-remediation.md` "Resume here" section.
- 2026-04-15: Drafted Mygrant Glass API integration setup form. Added User-Agent rule to `.claude/CLAUDE.md` and Integrations section here. Revised after Codex second-opinion review: switched UA contact email to `doug@pinkautoglass.com` (real routing address), dropped AWS/Next.js specifics from OMS framing, acknowledged Vercel Static IPs/Secure Compute as a future option in the IP answer, and upgraded enforcement from "documentation rule" to "code-level invariant" (central client + constant + test + repo guard required in first client PR).
- 2026-04-28: Audited Google Ads + admin dashboard for Denver vs Arizona launch readiness. Verified live Google Ads API access to customer `Pink Auto Glass` and found only one active campaign (`Keywords`) plus one paused `Performance Max`. Last-30-day Denver Google spend from admin metrics was $6,257.59 on 177 Google-attributed leads; Arizona market filter showed $0 paid spend but 5 Google-attributed leads, indicating Phoenix market plumbing exists before paid launch. Found bug in local admin sync status route `/api/admin/sync/google-ads`: returns `supabase is not defined`.
- 2026-04-28: Built Phoenix-specific negative keyword baseline from the Denver Google negatives export. Verified the site currently presents broader Phoenix-metro service coverage (20 AZ cities) in `src/app/arizona/page.tsx`, so the Phoenix trim removed Denver/Colorado geo negatives, kept generic/wrong-intent/competitor negatives, and added non-service Arizona + nearby spillover location negatives. Output files: `data/google-ads/phoenix-negative-keywords-upload.csv` and `data/google-ads/phoenix-negative-keywords-summary.md`.
- 2026-04-28: Added strict 4-city fallback negatives (`data/google-ads/phoenix-launch-4city-negative-keywords-upload.csv`), Phoenix launch ad copy (`data/google-ads/phoenix-launch-ad-copy.md`), and Phoenix launch build sheet (`data/google-ads/phoenix-launch-build-sheet.md`). Ran second-opinion reviews through Gemini and Codex. Both agreed the campaign strategy is broadly right, but flagged pre-launch blockers: Arizona replacement landing page still contains Denver/Colorado messaging, Arizona insurance page copy needs cleanup/proofread, and tracking/reporting consistency should be reconciled before paid launch.
- 2026-05-02: Completed a Denver-only main-site holistic audit using Google Ads, Search Console, on-site/admin analytics, leads, and call data. Gemini was used as the standing second-opinion reviewer on acquisition, CRO, and measurement sections. Core conclusion: paid search is carrying Denver, local SEO is weak for non-branded demand, and measurement/call attribution quality is the biggest blocker to scaling efficiently. Presentation artifact created at `docs/2026-05-02-denver-holistic-audit.html`.
- 2026-05-03: Doug approved a local-only secrets-safe OAuth flow for Search Console instead of pasting codes/tokens into chat. Created `GSC-SECRETS-SAFE-OAUTH-SPEC.md` to govern the implementation and required an ongoing rule/process so future secret-bearing auth flows stay off chat and out of git.
- 2026-05-03: Implemented the reusable local secret-safe OAuth pattern in `scripts/exchange-google-oauth-code.mjs`, added npm shortcuts (`oauth:gsc`, `oauth:ads`, `oauth:gbp`, `oauth:gsc:verify`), documented the process in `docs/SECURE-OAUTH-SECRETS-FLOW.md`, and tightened the workspace secrets protocol to forbid asking for OAuth codes/tokens in chat. Local verification succeeded immediately: `node scripts/verify-google-search-console-auth.mjs` returned `GSC auth OK` with access to `sc-domain:pinkautoglass.com`, which means the repo's current local Search Console token is already usable for the PAG audit.
- 2026-05-02: Started direct execution from the original brief. Added `MEASUREMENT-ATTRIBUTION-EXECUTION-SPEC.md`, created shared qualifying-call helper `src/lib/callQualifying.ts`, updated metrics/leads/call-attribution paths to use shared qualifying logic, removed heuristic time-correlation from canonical call attribution writes, added `direct_match_conflict` handling, and added overwrite-priority protection in `saveAttributionResults()`. Local `npm run build` passed. Shadow analysis over the last 30 days showed paid-attributed qualifying calls tightening from 139 under legacy logic to 69 under direct-evidence-only logic, with 223 now landing in `unknown` and 3 in conflict.
- 2026-05-02: Per Doug’s request, audited the 223-call `unknown` bucket before accepting the stricter baseline. Result: the drop is not safe to accept as “true performance” yet. Of the 223 unknown qualifying calls in the last 30 days, 94 already have `google_ads_call_match=true`, 47 already have `microsoft_ads_uploaded_at`, and 29 more tie to paid leads via same-phone matches with `gclid`/`msclkid` or paid `ad_platform` within 7 days. Only 39 remain unattributed call leads with no paid evidence on the matched lead row, and 14 look like repeat/referral callers. Conclusion: ~170/223 unknown calls likely represent attribution plumbing gaps, not real paid-performance loss.
- 2026-05-02: Ran Gemini second-opinion on the unknown-bucket audit; Gemini agreed the conclusion was sound and recommended a guardrail mindset because this is a source-of-truth metric change. Implemented the first canonical fix pass in `src/lib/callAttribution.ts`: deterministic `google_ads_call_match` / `google_ads_call_resource_name` now promote to `google_call_view`, `microsoft_ads_uploaded_at` now promotes to `microsoft_uploaded_call`, and conflicting deterministic evidence is surfaced explicitly instead of silently discarded. Updated `/api/admin/attribution/match-calls` summary/breakdown reporting to include the new methods. Local `npm run build` passed. Post-fix 30-day shadow run: 295 qualifying calls → 74 Google call-view matches, 49 Microsoft uploaded-call matches, 64 direct phone-click matches, 26 conflicts, and 82 remaining unknown (down from 223).
- 2026-05-02: Heartbeat follow-through: profiled the post-fix remainder and found the 82 unknowns now split into 29 `paid_lead_link_still_missing`, 39 `unattributed_call_lead`, and 14 repeat/referral patterns; the 26 conflicts are mostly deterministic dual-marker cases (24 have both Google call-view and Microsoft uploaded-call markers). Implemented a follow-on sync hardening in `src/lib/callLeadSync.ts` so call-lead creation/update no longer relies solely on `ringcentral_calls.ad_platform`: it now infers Google/Microsoft from deterministic markers and canonical attribution methods, while intentionally leaving dual-marker conflicts unattributed instead of overstating paid attribution. Local `npm run build` passed.
- 2026-05-02: Additional heartbeat hardening pass on `src/lib/callLeadSync.ts`: existing call leads with the correct paid platform are now eligible for click-ID/session enrichment from `website_session_id` instead of being skipped just because `ad_platform` was already populated. This targets the remaining “paid but still missing linkage” cases where leads already say Google/Microsoft but still lack `gclid` / `msclkid` or session linkage. Local `npm run build` passed.
- 2026-05-02: Added the missing cron endpoint `src/app/api/cron/run-attribution/route.ts` so the canonical resolver can finally be scheduled instead of remaining admin-only/orphaned. Route follows the existing cron auth pattern (`Bearer ${CRON_SECRET}`), defaults to a 7-day window, and can optionally dry-run with `saveToDatabase=false`. Local `npm run build` passed.
- 2026-05-02: Wired `/api/cron/run-attribution` into `vercel.json` at `0 14 * * *` so it runs one hour after the 13:00 UTC `sync-search-data` pass, and updated `CRON_SETUP.md` to match the real live cron inventory instead of the stale per-source endpoints. Local `npm run build` passed after the schedule/doc update.
- 2026-05-02: Ran a DB-safe `callLeadSync` simulation over `2026-04-02` → `2026-05-02` using service-role reads only. Result: `297` raw inbound 30s+ calls (`295` after filtering), `248` unique callers, `0` creates, `25` potential lead updates, `223` skips. All `25` update candidates were weak-platform leads (`ad_platform` null/direct/unknown) rather than click-ID/session enrichment wins, and all `25` overlap deterministic canonical attribution outcomes (`22` Microsoft uploaded-call markers, `3` Google call_view markers). That means the next leverage point is historical/backfill canonical attribution, not more `callLeadSync` logic.
- 2026-05-02: Executed the historical backfill path through `POST /api/admin/attribution/match-calls?startDate=2026-03-01&endDate=2026-05-02`. Dry run summary was clean: `687` qualifying calls → `202` `google_call_view`, `92` `microsoft_uploaded_call`, `125` `direct_match`, `42` `direct_match_conflict`, `226` `unknown`, `0` `time_correlation`. First write pass exposed a live schema bug: `ringcentral_calls.check_attribution_method` still rejected `microsoft_uploaded_call`, causing exactly `92` failures. Added migration `supabase/migrations/20260502_allow_microsoft_uploaded_call_attribution.sql`, applied it live via `scripts/run-migration-via-api.js`, reran the write pass, and the backfill completed cleanly with `saved: { success: 687, failed: 0 }`.
- 2026-05-02: Immediately after the backfill, reran the `callLeadSync` read-only simulation for `2026-04-02` → `2026-05-02` and found the expected downstream unlock: `83` potential updates (`68` weak-platform fixes + `15` same-platform enrichment passes, including `45` `gclid`, `10` `msclkid`, and `59` `website_session_id` fills). Applied the real `syncCallLeads()` write for that window: `0` created, `83` updated, `167` skipped. Final verification dry run dropped to `0` remaining updates and `248` skips, confirming the backfill + lead-sync remediation fully collapsed the pending weak-platform queue for that window.
- 2026-05-02: Ran a full-context Gemini audit on the remediation progress and then verified Gemini's two biggest concerns against code + live data. Result: Gemini was directionally right. `callLeadSync.ts` still writes lead session linkage into `leads.website_session_id`, while the most recent form leads are populating `leads.session_id`; live sample + count check since `2026-03-01` shows `session_id` on `195` leads, `website_session_id` on `59`, and only `4` with both. Separately, `metricsBuilder.ts` and `unifiedLeadsBuilder.ts` still only treat `google_call_view` and `direct_match` as canonical, so `microsoft_uploaded_call` and `direct_match_conflict` are not yet reflected consistently in dashboard logic. This reprioritized the next work: fix the lead-session field split and canonical dashboard-reader coverage before the GET-status route cleanup.
- 2026-05-02: Completed that post-Gemini hardening pass. `src/lib/callLeadSync.ts` now reads both `leads.session_id` and `leads.website_session_id` for existing linkage but writes recovered call session linkage into the live `session_id` field instead of reinforcing the legacy field. `src/lib/unifiedLeadsBuilder.ts` and `src/lib/metricsBuilder.ts` now treat `microsoft_uploaded_call` as canonical and deliberately collapse `direct_match_conflict` to `null` so fallback heuristics do not invent a paid winner. `/api/admin/attribution/match-calls` GET status reporting was also fixed to use the same qualifying-call scope/date bounds as the resolver and to report `conflictedMatches`. Local `npm run build` passed after both the reader/session-field changes and the GET-route cleanup.
- 2026-05-02: Ran a deeper architecture research pass on the four remaining attribution issues using external sources plus separate Gemini reviews per issue. Main takeaways: (1) keep conflict handling conservative for now — do not export dual-evidence conflicts to either ad platform until we have a firmer tie-break signal than today's mixed markers; (2) add a dedicated attribution-health snapshot + anomaly/guardrail monitor around unknown/conflict/freshness/method-share metrics rather than relying on ad-hoc reads; (3) finish the single-writer transition by stripping `ad_platform` writes out of `src/lib/callAttributionSync.ts` and leaving it as a fact-gathering Google evidence job only; (4) retire `leads.website_session_id` via an expand/contract path with backfill, zero-dependency verification, then drop. Gemini agreed strongly on monitoring, single-writer cleanup, and expand/contract retirement; on conflict policy it pushed for a hard Google tiebreak, but the safer project recommendation remains to keep dual-marker conflicts unresolved for bidding/export until we collect stronger sequence evidence.
- 2026-05-02: Implemented the first attribution-health monitoring pass locally. Added migration `supabase/migrations/20260502203213_create_attribution_health_snapshots.sql`, new helper `src/lib/attributionHealth.ts`, and cron route `src/app/api/cron/check-attribution-health/route.ts`. The new route builds a daily snapshot (raw inbound volume, qualifying calls, method counts, unknown/conflict rates, average confidence, freshness lag), saves it to `attribution_health_snapshots`, compares it to the trailing 7 snapshots with anomaly + guardrail rules, and emails admin alerts when thresholds are breached. Added the route to `vercel.json` at `15 14 * * *` and updated `CRON_SETUP.md`. Local `npm run build` passed. Remaining step before this is live: apply the new migration and deploy/schedule the route.
- 2026-05-02: Finished the next single-writer cleanup locally. `src/lib/callAttributionSync.ts` no longer writes `ad_platform: 'google'`; it now records deterministic Google evidence only (`google_ads_call_match` + `google_ads_call_resource_name`) and leaves derived attribution ownership to the canonical resolver in `src/lib/callAttribution.ts`. Local `npm run build` passed again. This removes the last known competing `ad_platform` writer from the call cross-reference job.
- 2026-05-02: Started the `leads.website_session_id` retirement path locally. Audited remaining usage and found one live form write still sending `website_session_id` through the booking RPC payload. Updated `src/app/api/booking/submit/route.ts` to send `sessionId` instead, added migration `supabase/migrations/20260502204500_backfill_leads_session_id_from_website_session_id.sql` to copy legacy values into `leads.session_id` where missing, and verified with `npm run build` (pass).
- 2026-05-02: Continued the retirement path with Gemini second-opinion reviews before and after the next cleanup step. Gemini agreed the safest next code cut was to remove the remaining `leads.website_session_id` compatibility read in `src/lib/callLeadSync.ts`, but caught one real follow-up risk: newly created call leads were still not writing `session_id`. Fixed both locally: `callLeadSync.ts` now reads only `existingLead.session_id` for lead linkage checks and writes `session_id: call.website_session_id || null` when creating new call-based leads, while leaving `ringcentral_calls.website_session_id` untouched. Local `npm run build` passed again.
- 2026-05-02: Executed the live rollout for the first `leads.website_session_id` retirement step. Applied `supabase/migrations/20260502204500_backfill_leads_session_id_from_website_session_id.sql` to production via `scripts/run-migration-via-api.js`, verified `SELECT count(*) FROM leads WHERE session_id IS NULL AND website_session_id IS NOT NULL;` returned `0`, then deployed a clean isolated Vercel production build containing only the approved attribution session-linkage code changes (`src/app/api/booking/submit/route.ts` + `src/lib/callLeadSync.ts`). Production deployment succeeded and was aliased to `https://pinkautoglass.com`. Immediate live checks: `curl -I https://pinkautoglass.com/book` returned `HTTP/2 200`, and there were no leads created in the 10-minute post-deploy window yet, so live session-linkage validation still needs the next real booking/call-created lead.
- 2026-05-02: Implemented the next conflict-resolution refinement locally after a fresh Gemini evidence review. `src/lib/callAttribution.ts` now lets deterministic Google call_view evidence beat unsupported `microsoft_ads_uploaded_at` bookkeeping, which should collapse the 39 fake dual-marker conflicts back to `google_call_view`. The same file now resolves mixed Google/Microsoft phone-click windows via `ringcentral_calls.website_session_id` when present, which is expected to resolve the 3 real ambiguous cases to the platform on the call-linked session while leaving truly sessionless mixed windows conservative. `src/lib/offlineConversionSync.ts` now skips Microsoft upload attribution for calls already carrying deterministic Google call_view evidence so the fake conflict pattern should stop growing. Local `npm run build` passed, and a post-change Gemini review found no blocker beyond the known sequencing caveat that Google evidence must land before Microsoft sync to avoid a rare early-upload edge case.
- 2026-05-02: Deployed that conflict-resolution refinement live to `pinkautoglass.com` from a clean isolated temp checkout linked to the production Vercel project so unrelated dirty-worktree changes were not shipped. Post-deploy smoke checks passed (`/book` returned `HTTP/2 200`; `/admin` returned `HTTP/2 401` as expected). Then reran the production attribution backfill for `2026-03-01` → `2026-05-02` through the live admin endpoint with writes enabled and got `saved: { success: 687, failed: 0 }`. The live summary matched the thesis exactly: `googleCallViewMatches` moved from `202` to `241` (+39), `directMatches` moved from `125` to `128` (+3), and `conflictedMatches` dropped from `42` to `0`. This confirms the 39 weak Microsoft conflicts collapsed to Google and the 3 true mixed-window cases resolved via call-linked session evidence.
- 2026-05-02: Audited existing RingCentral recording + outcome-linkage support for the call-analysis pilot. Verified the repo already has an authenticated recording fetch route (`src/app/api/admin/recording/[id]/route.ts`), RingCentral call sync persists `recording_id` on `ringcentral_calls`, and Denver filtering can be done via `classifyCallMarket(to_number)` in `src/lib/market.ts`. Live data check showed enough pilot volume in the last 60 days (`456` Denver inbound recorded calls, `69` won candidates, `137` conservative lost candidates). Wrote `RINGCENTRAL-RECORDING-PILOT-SPEC.md` to lock the first-pass workflow as an offline/local 20-call Denver pilot using local Whisper transcription and structured rubric scoring rather than a new production feature.
- 2026-05-02: Began executing the recording-analysis pilot. Built `analysis/ringcentral-pilot/pilot-call-cohort.csv` + `.json` with a cleaned 20-call Denver cohort (10 won / 10 lost) restricted to recorded inbound calls of at least 30 seconds and conservative lost labels (21+ days old, no completed status, no revenue, no Omega invoice evidence). Downloaded all 20 recordings locally using the existing RingCentral auth path, hitting a real 429 rate limit on the first burst and then resuming successfully with serialized backoff. Created `analysis/ringcentral-pilot/scorecards/pilot-scorecard.csv` as the structured review template. Local Whisper transcription is now running against the downloaded audio; first transcript artifact has been generated under `analysis/ringcentral-pilot/transcripts/`.
- 2026-05-02: Continued the pilot through transcript review and first-pass scoring. Fully scored the lost cohort, scored multiple clean win examples, and wrote `analysis/ringcentral-pilot/pilot-findings.md`. Main early signal: converted calls usually stay live until a concrete appointment is locked, while non-converted calls repeatedly leak at VIN/callback handoffs, same-day unavailability without a save attempt, early competitor release, or other optional-next-step endings. Also discovered a methodology issue on the won side: earliest-call and nearest-install-call selection both pulled contaminated transfer/follow-up/admin calls, so a transcript-aware heuristic (`analysis/ringcentral-pilot/reselected-won-candidates-v2.json`) is now being used to clean the final won cohort before final conclusions.
- 2026-05-02: Turned the pilot findings into an operational sales artifact: `analysis/ringcentral-pilot/CALL-CLOSE-SOP.md`. The SOP encodes the highest-leverage fixes from the call review: quote fast, keep control, save same-day misses, do not let VIN verification restart the sale, and require every viable call to end with a concrete booked or time-bound next step.
- 2026-05-02: Closed out the first documentation pass for the RingCentral pilot by adding `analysis/ringcentral-pilot/README.md`, which explains scope, artifacts, main findings, confidence/caveats, and the remaining won-cohort cleanup limitation. Current reusable package is now: cohort files, local transcripts/audio, scored sheet, findings memo, and rep SOP.
- 2026-05-09: Started automated quote engine MVP foundation. Added Mygrant SOAP inquiry client + guard, PlateToVIN client, quote identify API boundary, cash windshield pricing helper, smoke scripts, and migration draft `20260509_automated_quotes.sql`. Migration not applied yet; live API credentials still needed in `.env.local`.

## Attribution Remediation (in progress)

3-PR remediation of the call attribution model driving Google + Microsoft search ad spend. Background, full audit, decision log, and resume instructions live in `tasks/2026-04-14-attribution-remediation.md`.

**PR1 — MERGED to main 2026-04-15 (commit `3e9ac57`):**
- Stopped the dual-platform corruption: removed two `ad_platform` writes from `src/lib/offlineConversionSync.ts`
- Migration `20260414_expand_attribution_constraints.sql` applied to live DB (project `fypzafbsfrrlrrufzkol`) via the new `scripts/run-migration-via-api.js` helper. Constraints verified via `pg_constraint` query
- Dashboard query builders (`metricsBuilder.ts`, `unifiedLeadsBuilder.ts`) now SELECT and prefer `attribution_method` when set to a canonical value (`google_call_view` or `direct_match`). Backward-compatible no-op until PR2 ships
- Reviews: Claude self-review + Gemini SHIP-WITH-CHANGES + Codex MERGE

**PR2 — PARTIALLY SHIPPED 2026-05-02.** Resolver integration has materially advanced: deterministic Google/Microsoft markers now map into canonical attribution methods, shared `isQualifyingCall()` logic exists and is wired into the main readers/resolver, `time_correlation` has been removed from canonical writes, and overwrite-precedence protection is in place in `saveAttributionResults()`. Remaining PR2-adjacent work is mainly conflict policy, offline-upload allowlist review, and any further resolver cleanup. See the task file plus 2026-05-02 changelog entries above.

**PR3 — PARTIALLY SHIPPED 2026-05-02.** `/api/cron/run-attribution` now exists, is scheduled in `vercel.json`, and `CRON_SETUP.md` has been updated. Remaining PR3 work is backfill execution plus cleanup like dropping zombie `leads.website_session_id`.

## Known Issues — Attribution

- **Stale `POSTGRES_URL` credential** (Codex flagged 2026-04-14). Points at deleted project ref `ihbhwusdqdcdpvgucvsr` instead of live `fypzafbsfrrlrrufzkol`. `scripts/run-migration.js` is broken because of this. Workaround in place via `scripts/run-migration-via-api.js`. Real fix: pull fresh transaction-pooler URL from Supabase dashboard, `printf 'value' | vercel env add POSTGRES_URL development` (per the `\n` rule, never `echo`), `npm run env:pull`. ~5 minutes. Filed as a separate followup, not blocking attribution work.
- 2026-05-21: Investigating live review-request alert reporting 1 failed customer message out of 31 sends today. Located relevant flow in `/api/cron/process-drip`, `src/lib/drip/*`, and `src/lib/notifications/beetexting.ts`; next step is identify the exact failed lead/message and root cause.
- 2026-05-21: Confirmed alert source was the 6 PM MT review-request cron run. Database currently shows 30 `review_request` SMS rows sent and 1 retried row (`c353b353-7357-411c-90cf-e1d216ea7b45` for lead `f452b15d-f2f7-4185-b9ac-21554ad1bfa7`, phone `+13073101026`) rescheduled to 17:15 MT with `retry_count=1`; no current `failed` rows. Likely a one-recipient/provider send failure, not a system-wide outage.
- 2026-05-21: Repeated owner alerts after the initial batch are coming from the same review-request SMS row `c353b353-7357-411c-90cf-e1d216ea7b45` for `+13073101026`. It has now retried 3 times and remains `pending`; later cron runs report `0 sent successfully` because this single retried row is the only due message. Current alerting fires on retriable failures, not just terminal failures.
